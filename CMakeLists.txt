cmake_minimum_required(VERSION 3.10)
project( Hungarian )
find_package( OpenCV REQUIRED )
include_directories( include ${OpenCV_INCLUDE_DIRS} )
set(SOURCES main.cpp src/hungarian.cpp)
add_executable( Hungarian ${SOURCES} )
target_link_libraries( Hungarian ${OpenCV_LIBS} )